import { definitions } from "./types/options.types";

class Swagger {
  public initialOption: definitions;

  constructor(options: definitions) {
    this.initialOption = options;
  }
  public init() {}

  public set() {}
}

export default Swagger;
